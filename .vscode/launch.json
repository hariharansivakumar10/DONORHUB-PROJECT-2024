{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            // Use the following settings if using built-in PHP server
            "port": 9003,
            "pathMappings": {
                "/": "${workspaceFolder}"
            },
            // For Docker, uncomment and modify these settings:
            // "pathMappings": {
            //     "/var/www/html": "${workspaceFolder}"
            // },
            // "hostname": "localhost",
            // "port": 9003
        }
    ]
}